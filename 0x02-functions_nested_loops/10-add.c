#include "main.h"

/**
 * add - adds two integers.
 * @x: first parameter.
 * @y: second parameter.
 *
 * Return: result (sum of x and y)
 */
int add(int x, int y)
{
	int result;

	result = x + y;
	return (result);
}
