#include <stdio.h>

/**
 * main - Prints lowercase alphabets a-z
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	putchar(i);
	putchar('\n');
	return (0);
}
