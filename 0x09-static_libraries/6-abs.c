#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 * @v: the character
 *
 * Return: v if v > 0 and -v if v < 0
 */
int _abs(int v)
{
	if (v > 0)
		return (v);
	else
		return (-v);
}
